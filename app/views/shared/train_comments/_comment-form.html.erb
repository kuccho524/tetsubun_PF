<%= form_with model:[train, train_comment], url: train_train_comments_path(train), method: :post, remote: true do |f| %>
  <%= f.text_area :comment, placeholder: "コメントを入力してください。" %><br>
  <%= f.submit '送信', class: "btn btn-success" %>
<% end %>